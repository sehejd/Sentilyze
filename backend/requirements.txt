flask==3.0.3
flask-cors==4.0.2
# Pinned to a recent stable release - yfinance's compatibility with Yahoo's
# unofficial API changes constantly, and older pins go stale/broken fast.
# See utils/yf_client.py for how we mitigate Yahoo's rate limiting.
yfinance>=1.5,<2
requests==2.32.3
beautifulsoup4==4.12.3
pandas==2.2.2
numpy>=1.26,<3
textblob==0.18.0
vaderSentiment==3.3.2
praw==7.7.1
python-dotenv==1.0.0
google-generativeai==0.8.3

# Social/search trend momentum (scraping/social_trends.py). Best-effort:
# the app degrades gracefully if Google Trends rate-limits or this isn't installed.
pytrends==4.9.2

# Backtesting engine (backtesting/engine.py) needs pandas (above) for
# indicator math - no extra dependency required.

# Valuation calculator + peer-performance logistic regression
scikit-learn>=1.4,<2

# Optional: transformer-based FinBERT sentiment (see analysis/ml_sentiment.py).
# Not installed by default - it pulls in torch (~1GB+). Install separately with:
#   pip install -r requirements-ml.txt
# and set USE_FINBERT=true in .env to enable.
